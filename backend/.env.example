# PostgreSQL (VPS própria)
DATABASE_URL=postgresql+asyncpg://postgres:[password]@localhost:5432/app_financeiro

# OpenRouter (free tier — https://openrouter.ai)
OPENROUTER_KEY=

# App API key (auth single-user) — gerar: python -c "import secrets; print(secrets.token_urlsafe(48))"
APP_API_KEY=change-me-in-production

# Fernet key (criptografia de credenciais) — gerar: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=zH8jK2mN5pQ7rT9vW1xY3aB4cD6eF8gH0iJ2kL4mN6o=

# Web Push (VAPID) — opcional
VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_CLAIMS_EMAIL=mailto:heitormarchicursos@gmail.com

ENVIRONMENT=development
